В верху файла APP - необходимо прописать параметры базы - таблици создадтся сами

GET запросы на получение данных должен хранить в Heders keyheders : ВАШ КЛЮЧЬ
POST запросы (На загрузку файла на сервер) -  keyheders : ВАШ КЛЮЧЬ - должен быть в теле

Список с пагинацией не делал. (фактически это добавить количество строк в запросе и вводить через параметр)
Удаление файлы с сервере - не делал (с базы данных удаляются)
Ключь хранится в открытом виде в базе (В рабочей базе так не далал бы - это тест)
так же в таком виде не делал бы хранение файлов в рабочей базе. 

тест делался для понимания процессов 
1. Работа с БД - так как предоставляет Express
2. Работа с Rout (Http - запрос ) + Wrapper обвертка
3. Понимание async await (promice)
 